[server_local]
# Inline [server_local] nodes are supported by QX, but the built-in 已保存 pool
# is not addressable by resource-tag-regex. Prefer the tagged local resource in
# ServerRemote.private.conf.example when it must join Sub-Store All.
# shadowsocks=server.example.com:443, method=aes-256-gcm, password=CHANGE_ME, tag=Manual Node
